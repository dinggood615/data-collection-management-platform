services:
  tender-platform:
    build: .
    container_name: tender-platform
    restart: unless-stopped
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - platform_data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
volumes:
  platform_data:
